const arch = @import("arch");
const kernel = @import("kernel_common");

test "Terminal - print" {
    //kernel.terminal.print.printString("print test");
}

test "Terminal - printLog" {
    //kernel.terminal.print.printStringColor("printLog test", arch.TextColor.GREEN);
}
