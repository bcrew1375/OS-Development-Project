{
  "configurations": [
    {
      "name": "QEMU i386 remote (:1234)",
      "type": "gdb",
      "request": "attach",
      "executable": "${workspaceFolder}/build/kernelfull.o",
      "cwd": "${workspaceFolder}",
      "gdbpath": "/usr/bin/gdb",
      "target": "localhost:1234",
      "remote": true,
      "autorun": [
        "set architecture i386",
        "add-symbol-file ${workspaceFolder}/build/kernelfull.o 0x00100000"
      ],
      "preLaunchTask": "Run QEMU"
    }
  ]
}
